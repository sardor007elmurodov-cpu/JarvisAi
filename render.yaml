services:
  - type: web
    name: jarvis-cortex
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python vps_agent.py
    envVars:
      - key: PORT
        value: 5000
      - key: PYTHON_VERSION
        value: 3.10.0
